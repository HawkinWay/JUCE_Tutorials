# WaveformSynth

> x(t) = Asin(2pi * f * t)  
> t = n * Ts = n / fs  

> x(n) = Asin(2pi * (f / fs) * n)

## Nyquist Theorem

To recover original signal without distortion, the sample rate must higher 2 times than the highest frequncy

> fs > 2 * f


